version: '3.8'

services:
  flooz:
    build: .
    container_name: flooz
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
      - DATABASE_PATH=/app/data/flooz-budget.db
    restart: unless-stopped

volumes:
  budget-data:
    driver: local